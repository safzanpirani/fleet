# Implementation Plans

Generated by the `improve` skill on 2026-06-26, against commit `b6eab9b`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row in the table when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add a `bun test` suite for the quoting-proof core | P1 | M | — | DONE (landed as test/{ssh,config,cp,jobs}.test.ts by commit 5944999; no separate core.test.ts) |
| 002  | Stop `exec`/`spawn` from hijacking `--json/--raw/--wsl/--cwd` inside the command | P1 | S | 001 | DONE (2026-07-07; parseLeadingFlags — flags now go BEFORE the selector. 5944999's "strict flag parsing" was numeric-only and did NOT fix this) |
| 003  | Give `exec` a wall-clock timeout so a hung remote command can't hang the MCP server | P1 | S–M | 001 | DONE (commit 5944999; FLEET_EXEC_TIMEOUT + per-call timeoutMs, exit 124) |
| 004  | Reuse SSH connections via ControlMaster/ControlPersist | P2 | M | 001 | DONE (2026-07-07; controlOpts() in ssh.ts, FLEET_NO_SSH_MUX escape hatch; sshDiagnose deliberately un-muxed) |
| 005  | Author `CLAUDE.md` capturing architecture + load-bearing invariants | P2 | S | — | TODO |
| 006  | Expose the detached-jobs surface over MCP (`fleet_spawn`/`fleet_jobs`/…) | P2 | M | 001 (soft) | DONE (fleet_spawn/fleet_jobs registered in server.ts) |
| 007  | Design + spike the Windows detached-jobs backend | P3 | M–L | — | DONE (windowsSpawnScript in jobs.ts; README d0e2932 "Windows jobs now implemented") |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **002, 003, 004 depend on 001** — they each add or extend unit tests that live
  in the files 001 creates (`test/core.test.ts`, `test/ssh.test.ts`). Landing 001
  first means each fix ships with a regression test instead of bolting tests on
  later. If you must do 002/003/004 before 001, create the test file yourself
  following 001's structure (and note it in your status row).
- **004 modifies the `buildArgs` assertions that 001 writes** (it adds SSH
  control-socket options to the ssh argv). Do 001 → 004 in that order, or 004
  must update `test/ssh.test.ts` to match.
- **006 is soft-dependent on 001** — it can ship independently, but the job-core
  functions it exposes are better covered if 001's `test/jobs.test.ts` exists.
- **005 and 007 are independent** of everything else.

## Recommended sequencing

1. **001** (unblocks the correctness fixes with a safety net)
2. **002**, **003** (P1 bugs; small, high-confidence, now test-backed)
3. **004** (P2 perf; test-backed)
4. **005** (P2 docs; parallelizable any time)
5. **006** (P2 feature; surface parity for agents)
6. **007** (P3 spike; produces a design + prototype, not production wiring)

## Findings considered and rejected

So nobody re-audits these:

- **HTTP bearer-token-as-root model** — by design and well-implemented: constant-time
  compare (`http.ts:33-43`), `≥16`-char enforcement (`http.ts:59-62`), `127.0.0.1`
  default bind, `FLEET_MCP_READONLY` kill-switch, `destructiveHint` annotations.
  Not a vulnerability; the README is explicit that the token is a root credential.
- **`cua-driver` install via `curl … | bash` / `irm … | iex`** (`core.ts:409-421`) —
  the documented official installer, user-initiated, fetched over HTTPS from the
  trycua repo, run on the user's own hosts. By-design dual-use, not a finding.
- **Committed `fleet.config.json`** — verified to contain only SSH aliases /
  hostnames / a dashboard URL, no secrets. The personal variant is git-ignored
  (`fleet.config.local.json`).
- **Duplicated ANSI-vs-plain renderers** in `cli.ts` and `server.ts` — a legitimate
  presentation split (ANSI for humans, plain text for agents), not debt worth
  consolidating.
- **DX-2: GitHub Actions CI** — worth doing, but deferred by the maintainer this
  round. Revisit once 001 lands; a `bun run typecheck` + `bun test` gate is ~15 lines.
